#include <iostream>

#include "models.hpp"
#include "algorithm.hpp"

int main() {
    std::vector<std::vector<int>> matrix{
                                            {1,9,1},
                                            {2,1,1},
                                            {5,1,7}
                                        };

//    std::vector<int> ans = algorithm::HungarianImpl(matrix);

//    for(int i = 1; i < ans.size(); ++i){
//        std::cout << i << "--" << ans[i] << std::endl;
//    }
}
